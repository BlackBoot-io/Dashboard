import { useTranslation } from "react-i18next";
const Crowdsale = ({ data, loading }) => {
  const { t } = useTranslation();
  return <div className="balance custom-card"></div>;
};
export default Crowdsale;
